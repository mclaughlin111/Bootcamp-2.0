// App.test.tsx
import { render, fireEvent, getByText } from "@testing-library/react";
import App from "./App.js";

it("changes table state based on Gender toggle", () => {
  const { container } = render(<App />);

  expect(getByText);
});
